# A classic example of association rule mining

In this example, I utilized the Python pre build approach as well as my own attempt to build the Aprori algorithm.
The dataset used in this lab is attached.
